# 交易投研中心开发路径图

> 当前路径：`daily-trade-journal` 极简每日交易日记。
> 本文件由用户控制。任何方向、范围或状态修改都必须先展示精确文本并由用户确认。

## 一、目标

每天只完成两件事：

1. 说明上一已完成美股交易日发生了哪些脱敏实际动作。
2. 核对这些动作是否符合成交前已经明确确认的交易计划。

最终结果先在独立 Codex 任务中展示。只有用户对最终版本明确回复“确认写入”后，才写入 Obsidian 每日交易日记。

## 二、输入边界

允许读取：

- Longbridge 美股交易日历。
- 上一已完成 America/New_York 交易日半开区间内的只读 `order executions --history`。
- 项目内已确认的周计划和已确认日内修订。
- 上一自然日可访问、与本交易项目直接相关且由用户明确确认最终版本的交易计划讨论。

不允许读取：

- assets、positions 或 order history。
- quotes、行情、事件、新闻、估值或资金流。
- 盈亏、账户、现金、购买力或对账数据。
- SQLite、旧 Web UI、Bridge、publication 或历史看板数据。
- 凭据、Cookie、API key 或账户标识。

计划讨论只有在最终版本已经明确确认时才能作为证据。草稿、闲聊、推断、待确认内容、不可访问任务或与计划真相源冲突的内容不得升级为计划。

## 三、脱敏投影

投影只保留：

- underlying。
- `买入` 或 `卖出`。
- `正股`、`单股杠杆 ETF`、`0DTE 期权`、`其他期权`或`无法识别`。
- `按计划`、`偏离计划`或`无法核对`。

同一 underlying、action、tool 的拆分成交合并。

不得保存或展示：

- 数量、价格、成本、佣金或盈亏。
- 账户、订单、成交、请求或会话 ID。
- 原始券商响应。
- 具体期权合约身份。

0DTE 只按到期日与 review_date 机械相等判断，绝不能写成 Long Call。

## 四、计划对齐

- 只有成交前已确认且有效的计划精确匹配 underlying、action 和 tool，才标记“按计划”。
- 事前已确认计划明确给出不同动作、不同工具或明确禁止时，才标记“偏离计划”。
- 缺失、冲突、过期、未确认或不完整证据一律标记“无法核对”。
- 不得从持仓、盈亏、行情、交易习惯或模型常识反推计划。

## 五、Obsidian 写入

目标路径：

`25 投资交易/10 每日复盘/YYYY-MM-DD.md`

约束：

- 自动化先生成草稿，不自动写入。
- 用户必须在当前任务中对最终版本明确回复“确认写入”。
- 只写 successful `complete` 或 `empty`；`blocked` 不写。
- 只管理 daily-trade-journal managed block。
- 保留“我的补充”和所有其他用户手写内容。
- 写前检查并发变化。
- 写后依次完成文件系统回读和 Obsidian CLI 回读。
- 不使用 Bridge、outbox、receiver、receipt、SQLite、Feishu、Wiki 或远端同步。

## 六、自动化

目标：

- 每周二至周六 Asia/Shanghai 09:00 创建独立任务。
- 模型使用 LunaMax，推理强度 `max`。
- 每次只执行 `daily-trade-journal`。
- 首次启用前完成一次真实只读 shadow 和用户验收。

当前状态：

- 新自动化建议卡片已生成，等待用户在 UI 确认创建。
- 旧综合复盘自动化仍为 `PAUSED`。
- 新任务未创建前不得删除旧任务。
- 自动化配置成功不等于真实运行、人类验收或 Obsidian 写入成功。

## 七、不再维护

以下能力从仓库删除，不再作为 legacy capability 保留：

- 8765 Web UI 和 LaunchAgent。
- V1/V2 HTML renderer。
- TypeScript Web、publication 和本地 HTTP 服务。
- Bridge、outbox、receiver 和 receipt。
- SQLite 增量状态、迁移和周度执行统计。
- 市场环境、事件、估值、资金流和账户看板。
- 综合日度/周度复盘流程。
- Feishu/Wiki 写入流程。

历史实现仍可从 Git 历史和已保留 stash 恢复；恢复任何能力都需要重新讨论范围、架构和权限。

## 八、当前状态

- 产品方向：已确认。
- Skill 本地实现：已完成。
- 合成测试和 Skill 校验：已通过。
- 真实 Longbridge shadow：`NOT RUN`。
- Obsidian 真实日记写入：`NOT RUN`。
- 新自动化创建：`PENDING_USER_UI_CONFIRMATION`。
- Git commit：已完成。
- 功能分支推送：不执行。
- 合并和推送公开 `main`：已确认（2026-09-02）；完成状态以远端回读为准。
- 人类功能验收：`NOT RUN`。

## 九、审批记录

- 极简每日交易日记方向：已确认（2026-09-02）。
- Obsidian 确认后直接写入方向：已确认（2026-09-02）。
- 删除 legacy Web UI、Bridge、SQLite 和综合复盘代码：等待本次精确确认。
- 创建两个 commit：已确认（2026-09-02）；功能分支不推送。
- 将 `codex/minimal-daily-trade-journal` 以 fast-forward 合并到公开 `main` 并推送 `origin/main`：已确认（2026-09-02）。

注意：本文件记录用户批准的方向与外部写入边界；测试、push 或 CI 结果仍须单独回读，且不等于人工验收。
