# 交易投研中心开发路径图

> 当前路径：每日/周度单页统一交易复盘 Web UI V2 + 可审计交易计划 + Skill-first 增量状态层  
> 审批状态：Web UI V2、Skill-first、owner-only 本地持久化、单页日/周增量、执行质量复盘和交易计划构造方向已确认；人工浏览器 PASS、生产切换和自动化修改仍待单独确认  
> 本文件由用户控制。任何新增、删除或修改都必须先展示准确 diff 并取得确认。  
> 已授权在 `codex/trading-review-incremental-state` 分支实施单页日/周统一 V2、受限 Longbridge 公开日线只读能力及 SQLite v2→v3 追加迁移；这不授权生产切换、自动化修改、安装插件、其他 Longbridge 权限、券商写入或迁移知识库。

## 一、目标

把每日和周度交易复盘收敛到同一套简单、清晰、可交互的私人 Web UI，使用户既能完成每日盘前核对，也能在周度运行后回答：

1. 当前市场风险处于什么状态。
2. 昨天实际做了什么。
3. 当前持仓与既定计划是否一致。
4. 今天有哪些真正相关的事件和风险。
5. Codex 基于已验证事实给出的判断、执行检查项和待确认事项是什么。
6. 候选标的的观察、买入、减仓、退出和失效价格区间应如何由可审计证据构造。
7. 本周计划覆盖率、按计划执行率和计划胜率是多少。
8. 哪些盈利动作违反了计划，哪些亏损动作仍然严格执行了计划。
9. 哪几笔交易需要具体复盘，哪些规则需要保留、删除、重写或新增。
10. 下周有哪些与持仓和计划直接相关的事件与风险。

V2 的重点是提升决策可读性，不把数据管道、授权过程、模型调用和内部审计细节堆到主界面。

## 二、已确认的产品边界

### 2.1 数据来源

只使用 Longbridge 当前能够稳定、只读获取的数据。

每日默认数据范围：

- 当前账户资产与资金摘要。
- 当前持仓快照。
- 前一美股交易日的订单与成交。
- Longbridge 可获得的市场行情、事件日历和相关市场数据。
- 当前周计划和用户已经确认的每日增量。
- 当前持仓 underlying 与已确认候选池的 Longbridge 已完成日线；只用于构造和验证交易计划价格区间。

市场概览优先覆盖：

- 标普 500 或 Longbridge 可用的对应指数/代理标的。
- 纳斯达克 100 或 Longbridge 可用的对应指数/代理标的。
- 比特币。
- 原油。
- 黄金。
- 美债或 Longbridge 可用的利率代理标的。

规则：

- Longbridge 不支持的项目直接省略或标记为不可用。
- 不调用其他行情、新闻、宏观或资金流数据源补齐。
- 计划构造只允许调用 Longbridge 公开市场 `kline history`；每次最多 20 个标的，每个标的最多最近 550 个自然日，不扫描全市场。
- 日线必须为已完成的 1D K 线，时区、复权口径、严格递增顺序和 OHLCV 边界均可验证，且剔除未完成日线后至少保留 319 根。
- 技术结构统一使用 EMA20、EMA50、EMA200 和 ATR14，不使用 SMA。
- 最新 quote 只用于计算与已确认区间的距离，不得用于每日自动移动计划区间。
- K 线能力、复权口径、数据质量或历史覆盖不合格时计划构造状态为 blocked；不得自动切换 yfinance 或其他来源补齐。
- 资金流只展示 Longbridge 有明确字段定义的数据，不根据价格和成交量自行推断资金流向。
- 每项数据必须保留 `as_of`、市场时区和数据范围。
- 当前账户与持仓始终标记为读取时快照，不冒充收盘状态或完整账户对账。

### 2.2 Agent 与分析

- 分析由本次任务中的 Codex 在生成页面前完成。
- DeepSeek 从正式流程、Schema、看板状态和阻塞条件中移除。
- DeepSeek 不可用不再构成日报阻塞，也不在用户界面出现。
- Web UI 只展示 Codex 已生成的结构化分析结果。
- 页面内不嵌入 Agent、不提供聊天框、不提供 Prompt 输入、不提供模型选择或“运行 Agent”按钮。
- 页面本身不得调用 Codex、Longbridge 或其他远程接口。
- 如需重新分析，应重新运行每日复盘任务并生成新的私人 HTML。

Codex 分析必须区分：

- 已验证事实。
- 基于事实的解释。
- 条件式风险判断。
- 当前无法验证的缺口。

Codex 可以提供计划执行检查项，但不得自动下单，也不得把无条件买入、卖出、加仓或减仓指令包装成确定结论。

### 2.4 交易计划构造

- 交易计划由确定性技术证据、用户的交易意图、持有周期和风险边界共同构造。
- setup_type 固定包含 pullback、breakout、range、bottom_reversal 和 position_management。
- bottom_reversal 是右侧确认的抄底反转型：价格进入长期支撑或显著偏离区后，必须先出现确定性止跌或反转确认；没有确认时只能观察，不得生成可执行买入区。
- 建仓前计划必须区分观察区间、条件式进入区间、减仓/退出区间、失效区间和计划到期条件；建仓前不得生成加仓区间。
- 只有 Longbridge 订单/成交与当前持仓事实能够验证实际买入后，才允许生成新的 position_management draft。
- position_management draft 可以包含可选加仓区间，但必须重新经过用户确认；确认前发生的加仓不属于计划覆盖。
- 加仓依据必须是原交易逻辑仍然成立且出现新的有利结构，不得仅因为价格继续下跌而生成。
- 所有区间均以 underlying 价格表达；首版不构造期权合约权利金、行权价或到期日计划。
- 区间必须引用同一 evidence_id，并保留 source、as_of、timezone、adjustment、bars_used、ATR、关键位来源和数据状态。
- EMA 使用已完成日线的复权收盘价确定性计算；窗口、初始化方式和精度写入技术证据契约，禁止由模型估算。
- 系统生成的计划状态只能是 draft。用户明确确认后才生成不可变 confirmed 版本并记录 effective_at、confirmed_at、content_hash 和 supersedes_version。
- 行情或证据变化只能生成新 draft，不得静默修改已确认版本。
- 未提供风险边界、最低风险收益要求、成功条件、失效条件或到期条件时，不得把草案升级为可执行计划。
- 页面只展示条件式计划和检查项，不提供下单、改单、撤单或无条件买卖建议。

### 2.3 隐私和安全

不得进入页面或对话：

- 凭据、Cookie、API Key。
- 账户标识。
- 订单 ID、成交 ID。
- 原始券商响应。
- 成本、佣金和完整对账单明细。
- 完整机械日历或新闻馈送。

运行工件仍保存在：

`/private/tmp/trading-center-review-runtime/<run-date>/`

要求：

- JSON 与 HTML 均为仅所有者可读写。
- HTML 默认离线运行。
- 不加载外部字体、图片、脚本、iframe 或远程资源。
- 所有动态文本在渲染时进行安全转义。
- 账户事实继续在私有数据链路中校验并可供 Codex 形成脱敏风险判断，但不渲染账户标签、数值、基础币种、快照时间或金额显隐控件。

## 三、V2 页面信息架构

页面固定为以下六个区域。

### 3.1 市场风险雷达

位于页面最上方，默认一屏内可读。

展示：

- 主要市场资产的状态、变化、时间和简短风险标签。
- Longbridge 有明确口径时的市场资金流信息。
- 市场数据更新时间与快照边界。

账户资产、资金和盈亏字段保留在私有输入与校验链路中，不作为主界面模块或数值展示。

不展示详细授权日志、接口名、内部脚本状态或原始错误。

### 3.2 Codex 盘前判断

展示三个短区块：

- 市场状态：风险偏好、主要压力和支持因素。
- 组合状态：持仓集中度、对冲状态、计划覆盖和主要暴露。
- 今日执行检查项：触发条件、验证信号、失效条件和不行动条件。

结论必须能追溯到页面中的事实，不得用宽泛、随机或无法执行的建议填充。

### 3.3 昨日操作摘要

使用简短自然语言和标的级脱敏明细展示：

- 昨日实际订单和成交。
- 新增、退出、对冲或未完成动作。
- 与当前持仓变化的机械勾稽结果。
- 与既定计划的一致或偏离。

成功解析为空和数据读取失败必须明确区分。

### 3.4 当前持仓与计划

以持仓为主键，将计划直接关联到每个持仓或候选标的。

每项计划至少包含：

- plan_id、版本、draft / confirmed / superseded / expired 状态和生效时间。
- pre_entry / position_management 阶段、underlying、方向、setup 类型和计划周期。
- 技术证据来源、evidence_id、as_of 和数据质量。
- 建仓前展示观察、进入、减仓/退出和失效价格区间，不展示加仓区间。
- 实际买入验证后，允许展示独立的 position_management 草案；其中的加仓区间必须标明 draft / confirmed 状态。
- 最新 quote 与各区间的关系；该关系不得改变已确认区间。
- 确认信号、不行动条件、到期条件和事件风险。
- 原计划、实际进展、计划执行状态和下一步检查项。
- 当前缺口与无法机械验证的项目。

支持：

- 持仓 / 计划视图切换。
- 只看有变化。
- 只看接近触发或存在缺口。
- 展开查看证据和边界。

### 3.5 相关事件

只展示与以下对象直接相关的事件：

- 当前持仓 underlying。
- 当前周有效计划。
- 明确候选池。
- 对美股整体或组合暴露有明确风险通道的事件。

每项事件展示：

- 具体事件名称。
- Asia/Shanghai 时间。
- America/New_York 时间。
- 状态。
- 来源与数据状态。
- 对持仓或计划的主要风险通道。

时间排版不得依赖固定宽度的绝对定位。桌面端使用稳定列布局，窄屏自动转为上下排列。

### 3.6 数据说明

页面底部保留一个默认折叠的简短区域，只展示：

- 数据时间。
- 成功、部分成功或不可用的模块。
- 必须由用户处理的授权缺口。
- 当前快照和历史数据的边界。

内部脚本名称、模型路由、冗长校验状态和非用户可操作的系统信息不进入主界面。

### 3.7 日/周统一模式

- 每日和周度复盘共用一个 renderer、一个 HTML template、一个 `<main>` 和同一套视觉组件。
- 既有每日页面是唯一信息骨架，固定顺序保持为市场风险雷达、Codex 盘前判断、昨日操作摘要、持仓 × 计划、重要事件与时间轴、数据说明。
- 不建立独立周度 panel，不提供整页每日/周度切换；周度内容作为小型增量注入对应的每日组件。
- 市场与 Codex 区域只补充周度背景和纪律提醒；操作区域补充同视觉语言的本周操作摘要。
- 持仓 × 计划区域注入周度执行质量、计划复核和下周待确认草案；事件时间轴用“近日”和“下周”分组复用同一组件。
- 周度执行质量只展示计划覆盖率、按计划执行率、计划胜率和需复盘交易数，并在附近显示分子、分母和排除项。
- 页面不展示具体盈亏、收益率、TWR、现金流或完整归因。
- 每日运行可以读取最近周度版本用于展示，但不得重算、改写或创建周度 revision。
- 周度数据只由 weekly 运行更新；日分区发生修订时，旧周报派生显示 stale，内容保持不可变。
- 无周度版本时在原每日组件内显示“尚未生成”；无经过验证的每日 packet 时 weekly 运行 blocked，不得生成周度专用替代页面。

## 四、交互范围

V2 保持为单文件私人 HTML，不建设 SPA、不建设服务端，也不引入页面内 Agent。

首版只实现以下交互：

1. 持仓与计划视图切换。
2. 只看变化、只看接近触发、只看缺口。
3. 展开或收起证据与数据说明。

优先使用原生 HTML 和 CSS。只有在原生能力不能满足以上交互时，才允许加入少量固定、可审计的本地 JavaScript。

允许的 JavaScript 必须满足：

- 不发起网络请求。
- 不使用 `eval`、动态脚本或 `document.write`。
- 不使用不受控的 HTML 注入。
- 不包含 Agent、券商或模型调用能力。
- 通过 CSP 和自动化安全检查。

## 五、开发路径

### Path 0：建立边界与可回滚基线

目标：

- 明确 V2 的产品契约、数据契约和安全边界。
- 解决当前仓库无提交基线、文件整体未跟踪带来的审查和回滚风险。
- 不触碰用户未授权的其他文件。

交付：

- `docs/architecture/trading-center-web-ui-v2.md`
- 明确的文件所有权与改动范围。
- 状态与失败矩阵。
- 迁移和回滚方案。
- 完整浏览器人工验收清单。
- 经用户确认的 Git 工作方式和安全基线。

门禁：

- 架构报告准确内容必须先展示并得到单独确认。
- 不得因为创建路线图而自动创建架构报告或 Git 基线。

### Path 1：Longbridge 能力与字段审计

目标：

确认 Longbridge 实际能够为每日 UI 提供哪些数据，不提前假设字段存在。

检查范围：

- 账户资产与资金摘要。
- 持仓快照。
- 前一美股交易日订单与成交。
- 指数、比特币、原油、黄金、美债相关行情。
- Longbridge 提供的事件和资金流字段。

交付：

- `references/longbridge-dashboard-data-contract.md`
- 字段、口径、时间语义、权限和失败状态矩阵。
- 支持、部分支持、不支持三类清单。

门禁：

- 不得用测试夹具或旧看板证明实时接口可用。
- 不得安装或连接新的 Longbridge 插件，除非用户另行确认。
- 不支持的数据不会触发其他提供商回退。

### Path 2：V2 私有事实包与 Schema

目标：

将确定性数据与 Codex 分析分离，生成稳定的 V2 页面输入。

建议 Schema：

`trading-review-dashboard.v2`

一级模块：

- `market`
- `account`
- `codex_analysis`
- `operations`
- `positions`
- `plans`
- `events`
- `data_note`

要求：

- 每个模块独立记录 `status`、`as_of`、`source_scope` 和必要缺口。
- 单个模块失败不得伪装为空列表。
- Codex 分析不得修改确定性数值。
- Schema 中不再包含 DeepSeek 路由、状态或摘要字段。
- 私有事实包继续留在运行目录，不进入 Git 或知识库。

### Path 3：三种视觉方向与用户选择

在实现 V2 前：

1. 使用本地只读 HTTP 方式预览现有看板，避免 `file://` 浏览器限制。
2. 获取当前界面的视觉参考。
3. 基于同一份脱敏测试数据生成恰好三种视觉方向。
4. 展示三种方向的核心页面和交互差异。
5. 由用户明确选择一种方向。

三种方向都必须遵守同一数据、安全和信息架构，不以视觉探索改变数据口径。

未选择视觉方向前，不开始正式前端实现。

### Path 4：并行实现 V2 渲染器

V1 保持可运行，V2 使用独立文件实现。

建议新增：

- `references/dashboard-v2-contract.md`
- `assets/trade-review-dashboard-v2-standalone.html`
- `scripts/render_trade_review_dashboard_v2.py`
- `tests/test_render_trade_review_dashboard_v2.py`

目标：

- 修正日期、双时区和事件排版。
- 降低主界面的后台状态噪音。
- 建立清晰的视觉层级。
- 实现最小交互范围。
- 保持单文件、离线、owner-only 的交付方式。

在 V2 通过人工验收前：

- 不替换 V1。
- 不修改每日自动化默认入口。
- 不删除旧模板、旧渲染器或回滚工件。

### Path 5：接入 Codex 分析

目标：

由 Codex 基于脱敏、固定字段的事实包生成结构化分析。

分析输出包含：

- 市场状态摘要。
- 组合风险与对冲状态。
- 操作与计划偏差。
- 计划执行检查项。
- 事件影响。
- 未验证事项。

要求：

- 事实和判断分栏呈现。
- 每项判断关联证据或明确标记为推断。
- 数据不足时缩短结论，不自行补齐。
- Codex 失败时仍可展示确定性事实，但分析区明确不可用。
- 不再调用或检测 DeepSeek。

### Path 6：自动验证与人工验收

自动验证至少覆盖：

- V2 Schema 校验。
- 空、部分成功、失败、过期数据状态。
- HTML 转义与脚本安全。
- 无外部网络请求。
- 无 iframe、srcdoc、document.write 和动态脚本注入。
- 文件位于私人运行目录且权限仅限所有者。
- 日期、长标的名称和长事件名称布局。
- V1 与 V2 关键确定性数值一致性。
- DeepSeek 已从正式调用路径和用户可见状态中移除。

人工浏览器验收必须在架构报告中提供可执行清单，至少覆盖：

- 指定分支、构建和本地 URL。
- 一个可复用浏览器标签页。
- 1440×900、1280×800、390×844，100% 缩放。
- 正常、空、部分成功、失败和过期数据。
- 鼠标、键盘、焦点、滚动和展开收起。
- 持仓与计划筛选、证据展开和数据说明状态。
- 信息层级、日期可读性和移动端排版。
- Console 和 Network 检查。
- 重置步骤及 PASS / FAIL 记录位置。

人工验收在用户明确记录 PASS 前始终为 `PENDING`。

### Path 7：影子运行、切换与回滚

切换顺序：

1. 同一份脱敏事实包同时生成 V1 和 V2。
2. 对比关键数值、事件和计划状态。
3. 用户验收 V2。
4. 再单独确认修改 Skill 和每日自动化默认入口。
5. V1 至少保留一个完整运行周期作为回滚版本。
6. 稳定后再提出清理旧调用和旧文件的准确 diff。

任何阶段出现数据口径偏差、隐私问题、渲染失败或无法回滚时，继续使用 V1。

## 六、与个人知识中心的协作边界

交易投研中心负责：

- Longbridge 事实读取和脱敏。
- 每日复盘事实包。
- Codex 分析。
- Web UI V2。
- 用户确认后的投资记录输出包。

个人知识中心任务负责：

- Obsidian Vault 架构。
- 交易复盘在知识库中的目录位置。
- 笔记模板、索引、链接和生命周期。
- 实际写入、同步、迁移和回读规则。
- Feishu Wiki 的退役或过渡方案。

两个任务通过一个窄接口协作：

`confirmed-investment-review.v1`

该接口只包含用户确认后的脱敏内容，例如：

- 日期和复盘类型。
- 操作摘要。
- 计划与实际。
- 持仓级风险摘要。
- 相关事件。
- Codex 判断。
- 用户确认记录。
- 后续复盘事项。

它不包含账户标识、原始券商响应、订单/成交 ID、凭据或完整账户明细。

在个人知识中心架构确认前，本项目只定义接口，不决定 Obsidian 目录，也不进行双写。Feishu Wiki 是否停用必须由迁移任务单独确认并完成目标端回读后决定，不作为 Web UI V2 的前置阻塞。

## 七、首版不做

- 页面内 Agent 或聊天功能。
- 页面直接请求 Longbridge。
- 自动下单、改单或撤单。
- 多用户系统和登录系统。
- 云端部署。
- 实时推送行情。
- 第三方行情或新闻数据补齐。
- 复杂图表工作台。
- 长期保留完整券商原始数据。
- Plugin、MCP server、Codex App、marketplace 或常驻后台服务。
- 页面直接访问 SQLite、Longbridge 或 Codex。
- 在本项目中设计 Obsidian Vault 架构。
- 未经确认修改 Feishu Wiki 同步流程。

## 八、Skill-first 增量状态层

### Path 8：健康基线与仓库恢复

目标：

- 从 codex/trading-review-semantics@3e9bafb 建立健康、可审查的本地分支。
- 当前损坏的 web-ui-v2 只作为恢复证据，不修补其 .git。
- 逐文件迁入已确认 V2，不复制 Longbridge 日志、runtime、records、broker 数据或缓存。
- 收敛 Skill、README、引用和模板中的账户概览、42/58、事件全宽、Codex-only 与知识交接语义。

拟议实施分支：

codex/trading-review-incremental-state

门禁：

- 不 push、不 PR、不合并、不修改自动化。
- 先恢复 V1/V2 测试、Skill quick validation、隐私扫描和回滚线。

### Path 9：owner-only SQLite 状态内核

目标：

- 在 Git、Obsidian、Wiki 和 /private/tmp 之外保存增量事实。
- 允许持久化净资产、现金、购买力和精确仓位数量。
- 永久禁止账户标识、订单或成交 ID、成本、佣金、凭据、原始响应和完整日历进入数据库。

拟议默认路径：

/Users/archer/Library/Application Support/MarsTradingCenter/trading-review.sqlite3

要求：

- 父目录 0700；数据库、WAL、SHM 和备份 0600。
- 拒绝符号链接、Git 或 Vault 内路径和权限过宽的现有文件。
- 使用 SQLite 事务、WAL、单写者、分区状态、内容 hash、revision 和 user_version。
- 每次迁移前创建 owner-only 备份；迁移失败保留旧库并 blocked。
- 首版不自动删除私有运行目录或迁移备份。

### Path 10：每日与周度增量编排

按需计划构造：

- 只在用户要求创建或修订计划时读取合格的 Longbridge 已完成日线并生成确定性技术证据。
- 根据 setup 类型、关键位、ATR 缓冲、确认信号和用户风险约束生成 draft；不满足质量门或风险收益门时输出观察/不行动结论，不伪造买卖区间。
- bottom_reversal 必须先满足止跌/反转确认门；只进入超跌或支撑区域不能单独生成买入触发。
- pre_entry 阶段禁止生成 add zone。
- 验证实际买入后才可进入 position_active，并生成新的 position_management draft；系统不得自动确认该版本。
- 用户确认后才写入不可变计划版本；旧版本只标记 superseded 或 expired，不覆盖、不删除。

每日：

- 刷新当前 assets、positions、市场快照和相关事件。
- 前一美股交易日订单或成交按 America/New_York 半开窗口分区。
- complete 或成功 empty 分区复用；partial、stale、blocked 重试。
- 周计划或已确认增量 hash 未变化时不重复解析。
- Codex 分析按 facts_hash + plan_hash + analysis_contract_version 缓存。
- 只更新最新 quote 与已确认计划区间的距离和状态，不重新计算或改写价格区间。
- 未确认 draft 可显示为待确认，但不得计入计划覆盖率或执行率。

周度：

- 用 Longbridge 交易日历确定预期交易日。
- 复用每日分区，只补缺失交易日和周度专属接口。
- 周初或周末比较只使用当时持久化快照，不用周五当前快照反推。
- eligible trade episode 固定为 America/New_York 市场日期 × underlying × side；拆单和期权先聚合到 underlying，取消未成交、FX、IPO 和公司行动不进入分母。
- 只使用交易发生前有效的 confirmed 计划版本，将 episode 分类为 covered / uncovered、compliant / non_compliant / unassessable 和 success / failure / open / flat / unverifiable。
- 原始成交价格只在 owner-only 私有运行目录内用于分类；SQLite 和 Web UI 不保存或展示实际成交价格、成本、佣金或具体盈亏。
- 未在加仓发生前确认 position_management 版本的，加仓 episode 记为 uncovered 或 non_compliant。
- 计划覆盖率 = 有事前 confirmed 计划的 eligible episode / 全部 eligible episode。
- 按计划执行率 = fully compliant episode / assessable covered episode；必须与覆盖率同时展示，避免通过缩小分母美化结果。
- 计划胜率 = success / (success + failure)；open、flat 和 unverifiable 不进入胜率分母，但必须展示数量。
- 亏损但严格执行失效/止损规则可记为 compliant + failure；盈利但违反计划必须记为 non_compliant 并进入需复盘列表。

两轴状态始终保留：

- data_status：complete、partial、empty、stale、blocked。
- confirmation_status：pending、confirmed。

### Path 11：单页日/周 Web UI 与 SQLite v3

目标：

- 让周度执行质量进入既有每日 Web UI V2 和同一个 owner-only SQLite，不建立独立周度页面。
- 保持每日和周度各自独立的更新频率、版本和确认状态。
- 保存可审计的计划版本和价格区间，同时永久排除原始成交价、成本、佣金和具体盈亏。

实施：

- 周度固定 Schema 只提供注入既有日度组件所需的执行质量、复核、下周草案、事件和 freshness；renderer 不复制日/周内容树。
- 从 SQLite v2 追加迁移到 v3，新增 plan_versions、plan_zones、trade_episode_assessments 和 weekly_execution_metrics。
- plan_versions 保存版本、pre_entry / position_management 阶段、确认时间、证据 provenance 和内容 hash；position_management 必须引用已验证的初始买入 episode 和原建仓计划版本，但不得保存原始成交价格或成交 ID。
- plan_zones 保存结构化条件式价格区间；add 类型只允许属于 position_management 版本，pre_entry 版本出现 add zone 时 Schema 校验失败。
- trade_episode_assessments 只保存计划覆盖、执行合规、计划结果和偏差分类，不保存原始成交价格。
- weekly_execution_metrics 保存覆盖率、执行率、计划胜率、各分母计数和需复盘交易数量。
- 既有 weekly_performance、weekly_attributions 和 weekly_cash_flow_aggregates 表只作为历史兼容数据保留；新运行不刷新，新 UI 不渲染。
- 每日 runner 对周度表零写入；weekly runner 只追加新 revision。
- 通过每日 partition revision/hash 派生周报 freshness。
- v2→v3 迁移前创建 owner-only 备份；迁移或检查失败时回滚并继续使用 v2，不删除任何旧行。

门禁：

- 不自动切换现有自动化。
- 不把 partial、stale 或 blocked 模块升级为 complete。
- 不保存通用 JSON、费用佣金、具体期权合约或原始券商响应。
- W35 真实 shadow 必须如实保留计划权威缺失导致的 blocked 指标；完整人工浏览器验收通过后，才可提出生产切换。

### Path 12：Plugin 决策门

本阶段不 Plugin 化。只有以下条件至少满足两项时重新评估：

1. Web UI 需要成为 Codex 长期交互 App。
2. 多任务需要通过 MCP 查询同一本地状态库。
3. 出现两个以上需要统一安装的交易 Skill。
4. 需要 marketplace 统一安装、升级和分享。
5. 需要 hooks、常驻服务或跨项目生命周期管理。

未来 Plugin 必须包装现有 trading-center-review，不得要求重写 Skill 或迁移数据库格式。

## 九、完成定义

Web UI V2 只有同时满足以下条件才算完成：

- Longbridge 数据契约已经按实际能力验证。
- V2 Schema 和渲染器通过自动检查。
- DeepSeek 已退出正式流程和用户可见状态。
- 页面没有外部请求或敏感数据泄漏。
- 市场、操作、持仓计划和事件能够清晰阅读；账户事实保留在私有数据链路，不作为主界面独立模块展示。
- Codex 判断与确定性事实明确分离。
- 用户完成规定的浏览器验收并明确记录 PASS。
- 自动化切换经过单独确认。
- V1 回滚路径仍然有效。
- 同一 complete 交易日重复运行可证明 cache hit，且不会复制事实行。
- partial、stale、blocked 分区按合同重试，不被缓存为成功。
- 周度结果由预期交易日分区和周度专属接口机械聚合。
- SQLite 路径、权限、迁移、并发锁、revision 和禁止字段测试通过。
- 数据库、Web UI、运行 manifest 和知识交接包遵守各自不同的隐私边界。
- 每日与周度使用同一 renderer、template、`<main>` 和交互语言；不存在独立周度 panel 或整页周期切换。
- 每日运行不重算或改写周度结果。
- 周度依赖发生变化时，旧版本显示 stale，只有 weekly 运行才能生成新 revision。
- 用户可以从单一标的、交易意图、持有周期和风险边界生成一份有证据来源的条件式价格区间草案。
- 同一 evidence_id 能机械复算计划区间，技术结构只使用 EMA20、EMA50、EMA200，不出现 SMA 字段或文案，价格区间不是 Codex 自由生成的数字。
- bottom_reversal 在没有止跌或反转确认时只能输出观察状态。
- pre_entry 计划不得包含加仓区间；实际买入后生成的 position_management 加仓区间保持 draft，只有用户再次确认后才计入后续加仓计划覆盖率。
- draft 不计入计划覆盖；confirmed 版本不可静默修改，交易只能匹配发生前已经生效的版本。
- 日度运行只更新当前价格与计划区间关系，周度运行才更新执行质量统计。
- 页面不展示具体盈亏、收益率、TWR、现金流或完整归因。
- 数据不足、复权口径不明、风险边界缺失、区间冲突、证据过期、区间跳空穿越、部分成交或无法取得成交事实时均按真实状态失败关闭或标记 unassessable。
- SQLite v1→v2→v3 迁移保留全部旧 runs、partitions、周度表和事实行；旧盈亏相关表不再刷新或渲染。
- W35 partial/pending 状态在数据库、HTML 和 manifest 中一致。

## 十、当前未决事项

以下事项留待对应阶段确认，不在本路线图中提前冻结：

- Longbridge 实际支持的账户、资金流和宏观代理字段。
- Longbridge `kline history` 的实际复权参数、时间戳和完成日线判定；核验失败时计划构造保持 blocked。
- 使用现有 Longbridge CLI、Skill 还是独立适配器作为正式读取入口。
- 首版交互是否需要少量本地 JavaScript。
- `confirmed-investment-review.v1` 的最终字段。
- Obsidian 路径、模板和写入方式。
- Feishu Wiki 的具体退出时间。
- 本机 FileVault 状态及不做应用层加密的剩余风险。
- 私有运行目录和数据库迁移备份未来是否采用固定 TTL。

## 十一、审批记录

- 路线图内容：已由用户确认。
- 创建 `开发路径图.md`：已由用户批准。
- Web UI V2 架构：已批准采用方案 2；账户概览从主界面移除，重要事件与时间轴改为全宽模块。
- Web UI V2 实施：已授权在 `codex/web-ui-v2` 分支继续；人工浏览器 PASS 与生产切换仍未完成。
- Skill-first：已确认；本阶段不创建 Plugin、MCP、App 或 marketplace。
- 本地持久化：已允许 owner-only 保存净值、现金、购买力和精确仓位数量；禁止字段保持不变。
- 增量状态层具体架构、仓库恢复、数据库实施与自动化切换：仍按各自准确 diff 和验收门执行。
- 日/周统一 Web UI V2：用户于 2026-08-30 确认采用同一 UI、同一数据库和独立更新频率。
- SQLite v2：批准显式周度表、v1 备份迁移及 W35 r3 脱敏事实入库；不授权自动化或生产切换。
- 单页日/周与执行质量：用户于 2026-08-30 确认保留既有每日 UI 作为唯一骨架，周度只注入计划覆盖率、按计划执行率、计划胜率、需复盘交易和下周增量，不展示具体盈亏。
- 交易计划构造与价格区间：用户于 2026-08-30 确认采用受限 Longbridge 已完成日线、EMA20/50/200、右侧确认的 bottom_reversal、draft→confirmed 版本门和 SQLite v3 结构化投影；pre_entry 不含加仓区间，实际买入验证后才可生成待再次确认的 position_management 加仓计划。
- Longbridge `kline history`：已授权当前持仓 underlying 与已确认候选池的受限公开日线只读；不授权 yfinance 回退、期权合约价格构造或任何券商写操作。
- Longbridge 新插件或新权限：未授权。
- 自动化切换：未授权。
- Obsidian 写入：由个人知识中心任务管理。
