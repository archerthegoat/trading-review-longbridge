## 当前方向（2026-09-03）

本节为当前方向；下文与本节冲突的旧设计仅作历史记录。

2026-09-05 已批准的证据优先流程与周一至周五 09:00 日程，以《daily-trade-journal v2 架构报告》第 10 节为准；下文冲突内容仅作历史记录。

- 定位为以对话采访为核心的交易复盘日记助手，持续维护持仓管理和计划买入两类记录。
- 日常读取旧记录，采访并梳理操作与想法，按标的记录交易工具和具体计划，确认后保存到 Obsidian。
- 技术讨论以 PA 为主，区分日线背景与小时执行；按用户请求读取对应周期数据复验，区分自述与已核验事实。
- 用户驱动的宏观研讨可维护带 `as_of`、证据状态和失效条件的当前宏观假说，并按“油价与通胀风险 → 短端定价 → 信贷条件 → 长端与 AI 融资”映射市场状态；宏观结论本身不构成交易计划，只有明确到标的、买入或卖出、工具和条件的变化，经用户确认后才进入当前计划。
- 默认使用 Longbridge CLI 获取数据并核对持仓；其他券商按需映射相同字段含义，不预建通用适配框架。
- 周末回顾计划实施情况，继续有效的计划跨周沿用，确认作废后归档。
- 不把机械计划对齐、固定问卷或交易结果评价作为记录的前置条件。
- 保留隐私、原文保护、写入确认和回读校验，不恢复 Web UI 或数据库。

# 交易投研中心开发路径图

> 当前路径：`daily-trade-journal` 计划对齐、用户采访与 Obsidian 每日交易日记。
> 本文件由用户控制。任何方向、范围或状态修改都必须先展示精确文本并由用户确认。

## 一、目标

每天只完成四件事：

1. 说明上一已完成美股交易日发生了哪些脱敏实际动作。
2. 核对这些动作是否符合成交前已经明确确认的交易计划。
3. 采访用户当日交易想法、计划内外决策及下一交易日调整。
4. 在用户确认最终草稿后写入 Obsidian。

自动化在当前 Codex 任务中发起采访并等待用户回复。只有用户对最终版本明确回复“确认写入”后，才写入 Obsidian 每日交易日记。

## 二、输入边界

允许读取：

- Longbridge 美股交易日历。
- 上一已完成 America/New_York 交易日半开区间内的只读 `order executions --history`。
- Obsidian `25 投资交易/30 交易计划/当前交易计划.md` 中的动态当前计划，以及 `25 投资交易/30 交易计划/历史快照/` 中用于事前核对的计划历史快照。
- 当前任务中用户对当日交易想法的回答；该回答只作为事后日记，不作为事前计划证据。

不允许读取：

- assets、positions 或 order history。
- quotes、行情、事件、新闻、估值或资金流。
- 盈亏、账户、现金、购买力或对账数据。
- SQLite、旧 Web UI、Bridge、publication 或历史看板数据。
- 凭据、Cookie、API key 或账户标识。

计划讨论只有在最终版本已经明确确认时才能作为证据。草稿、闲聊、推断、待确认内容、不可访问任务或与计划真相源冲突的内容不得升级为计划。
计划版本只有在 `confirmed_at <= execution_at` 时才可用于计划对齐。采访内容和日记确认不得追溯改变事前计划状态。

## 三、脱敏投影

日记投影只保留：

- underlying。
- `买入` 或 `卖出`。
- 期权成交额外保留 `0DTE 期权` 或 `其他期权`；普通证券不展示工具标签。
- `按计划`、`偏离计划`或`无法核对`。

日记投影维持原聚合；私有期权核对预览额外保留到期日、认购/认沽和行权价，按完整合约及买卖方向合并拆分成交，不合并不同合约。

不得保存或展示：

- 数量、成交价格、成本、佣金或盈亏。
- 账户、订单、成交、请求或会话 ID。
- 原始券商响应。
- 完整期权代码。到期日、认购/认沽和行权价的组合仅用于私有本地核对预览，不进入 Git、日志或 Obsidian 日记。

0DTE 只按到期日与 review_date 机械相等判断，绝不能写成 Long Call。

## 四、计划对齐

- 只有成交前已确认且有效的计划精确匹配 underlying、action 和 tool，才标记“按计划”。
- 事前已确认计划明确给出不同动作、不同工具或明确禁止时，才标记“偏离计划”。
- 缺失、冲突、过期、未确认或不完整证据一律标记“无法核对”。
- 不得从持仓、盈亏、行情、交易习惯或模型常识反推计划。

## 五、交易想法采访

- 每次运行必须先展示脱敏事实和计划核对，再提出最多两个问题并等待用户回答。
- 有成交时询问计划内外决策、原因、做得好与不好之处和下一交易日调整。
- 无成交时询问主动等待或计划未触发的原因，以及下一交易日调整。
- 用户回答属于事后叙事，不得追溯升级事前计划。
- 未完成采访时不得生成最终日记，不得请求写入确认。

## 六、Obsidian 写入

目标路径：

`25 投资交易/10 每日复盘/YYYY-MM-DD.md`

约束：

- 自动化先生成草稿，不自动写入。
- 用户必须在当前任务中对最终版本明确回复“确认写入”。
- 券商事实状态与用户日记状态分别记录。
- 券商事实为 `blocked` 时仍可采访；只有用户确认后才可写入，且必须在日记中明确保留“事实核对阻塞”，不得表示为成功或空成交。
- 只管理 daily-trade-journal managed block。
- 保留“我的补充”和所有其他用户手写内容。
- 写前检查并发变化。
- 写后依次完成文件系统回读和 Obsidian CLI 回读。
- 不使用 Bridge、outbox、receiver、receipt、SQLite、Feishu、Wiki 或远端同步。

## 七、自动化

目标：

- 每周二至周六 Asia/Shanghai 09:00 在当前任务中触发 heartbeat。
- 模型使用 LunaMax，推理强度 `max`。
- 每次只执行 `daily-trade-journal`。
- 启用前完成确认计划迁移、第二次真实只读 shadow 和用户验收。

当前状态：

- 新自动化建议卡片已生成，等待用户在 UI 确认创建。
- 旧综合复盘自动化仍为 `PAUSED`。
- 新任务未创建前不得删除旧任务。
- 自动化配置成功不等于真实运行、人类验收或 Obsidian 写入成功。

## 八、不再维护

以下能力从仓库删除，不再作为 legacy capability 保留：

- 8765 Web UI 和 LaunchAgent。
- V1/V2 HTML renderer。
- TypeScript Web、publication 和本地 HTTP 服务。
- Bridge、outbox、receiver 和 receipt。
- SQLite 增量状态、迁移和周度执行统计。
- 市场环境、事件、估值、资金流和账户看板。
- 综合日度/周度复盘流程。
- Feishu/Wiki 写入流程。

历史实现仍可从 Git 历史和已保留 stash 恢复；恢复任何能力都需要重新讨论范围、架构和权限。

## 九、当前状态

- 产品方向：已确认。
- 现有 Skill 实现：已完成，但缺少确认计划适配器和强制采访。
- 首次真实 Longbridge shadow：`PARTIAL`；交易日历和成交读取完成，计划对齐与采访未完成。
- 已确认计划迁移：`PENDING`。
- 第二次真实 shadow：`NOT RUN`。
- Obsidian 真实日记写入：`NOT RUN`。
- 新自动化替换：`PENDING`。
- Git 集成：当前 `main` 与 `origin/main` 一致；本次改动尚未开始。
- 人类功能验收：`PENDING`。

## 十、审批记录

- 极简每日交易日记方向：已确认（2026-09-02）。
- Obsidian 确认后直接写入方向：已确认（2026-09-02）。
- 删除 legacy Web UI、Bridge、SQLite 和综合复盘代码：等待本次精确确认。
- 创建两个 commit：已确认（2026-09-02）；功能分支不推送。
- 将 `codex/minimal-daily-trade-journal` 以 fast-forward 合并到公开 `main` 并推送 `origin/main`：已确认（2026-09-02）。

注意：本文件记录用户批准的方向与外部写入边界；测试、push 或 CI 结果仍须单独回读，且不等于人工验收。
